# clinical-state-annotation — Environment variables example
#
# Copy this file to `.env` and fill in actual values.
# The `.env` file is gitignored — never commit secrets.
#
# This Level 0 release does not require any secrets to operate.
# `.env` slots are reserved for future Level 1+ tooling.

# Reserved: document hash salt for future Level 1 releases (NOT used in Level 0)
# DOC_HASH_SALT=replace_with_32+_character_random_secret_DO_NOT_USE_THIS_DEFAULT
